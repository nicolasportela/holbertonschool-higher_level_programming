#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    if len(tuple_a) == 0:
        tuple_a[0
    elif len(tuple_a) > 2:
    
    if len(tuple_b) < 2:

    elif len(tuple_b) > 2:

    add1 = tuple_a[0] + tuple_b[0]
    add2 = tuple_a[1] + tuple_b[1]
    print("({:d}, {:d})".format(add1, add2))
